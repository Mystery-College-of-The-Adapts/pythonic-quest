#include <iostream>
#include "stack.h"
using namespace std;


int main()
{ /*
	stack<int>num; 
	num.push(10);
	num.push(20);
	num.push(30);
	num.push(5);


	cout << " num.size() =  " << num.size() << endl;

	while (!num.isempty())
	{
		cout << "num.top() = " << num.top() << endl;     // access the top element 
		num.pop();										// and then pop it
	}


	cout << " num.size() =  " << num.size() << endl; */

	system("pause");
	return 0;

}